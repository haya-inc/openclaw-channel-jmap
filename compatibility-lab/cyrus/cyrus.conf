START {
  recover cmd="ctl_cyrusdb -r"
}

SERVICES {
  imap cmd="imapd" listen=1143 prefork=1 maxchild=20
  http cmd="httpd" listen=8080 prefork=1 maxchild=20
}

EVENTS {
  checkpoint cmd="ctl_cyrusdb -c" period=30
}
